\begin{problem}
    El peso $p$ en newtons (\si{N}) de un cuerpo depende de su masa $m$ en kilogramos, y se modela mediante la expresión $p = g \cdot m$, tal que $g$ es una constante que depende del planeta donde se encuentre el cuerpo.

    En Mercurio, el valor de $g$ es aproximadamente \SI{3.7}{m/s^2}, de manera que se puede establecer el siguiente gráfico:

    \begin{center}
    (imagen)
    \end{center}

    Si el valor de $g$ en la Tierra es aproximadamente \SI{10}{m/s^2}, ¿cuál de los siguientes gráficos podría representar el peso en función de la masa en Mercurio y en la Tierra?

    \begin{enumerate}[label=\Alph*.]
        \item (imagen)
        \item (imagen)
        \item (imagen)
        \item (imagen)
    \end{enumerate}
\end{problem}
